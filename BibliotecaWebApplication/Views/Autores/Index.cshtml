@model IEnumerable<BibliotecaWebApplication.Models.Autor>

@{
    ViewData["Title"] = "Index";
}

<h1>Autores</h1>

<p>
    <a asp-action="Create" class="btn btn-create mb-3">
        <i class="bi bi-plus-circle"></i> Create New
    </a>
</p>
<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Buscar por nombre o apellido: <input type="text" name="searchString"   />
            <input type="hidden" name="sortOrder" value="@ViewData["CurrentSort"]" />
            <input type="submit" value="Buscar" class="btn btn-primary" />
        </p>
    </div>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Apellidos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nacionalidad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Apellidos)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nacionalidad)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.AutorId" class="btn btn-primary btn-sm">
                        <i class="bi bi-pencil-square"></i> Edit
                    </a>
                    <a asp-action="Details" asp-route-id="@item.AutorId" class="btn btn-primary btn-sm">
                        <i class="bi bi-info-circle"></i> Details
                    </a>
                    <form id="delete-form-@item.AutorId" asp-action="Delete" asp-route-id="@item.AutorId" method="post" class="d-inline">
                        <button type="button" class="btn btn-danger btn-sm" onclick="confirmDelete('@item.AutorId')">
                            <i class="bi bi-trash-fill"></i> Delete
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
    </div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    function confirmDelete(authorId) {
        Swal.fire({
            title: "Are you sure?",
            text: "You won't be able to revert this!",
            icon: "warning",
            showCancelButton: true,
            confirmButtonColor: "#3085d6",
            cancelButtonColor: "#d33",
            confirmButtonText: "Yes, delete it!"
        }).then((result) => {
            if (result.isConfirmed) {
                Swal.fire({
                    title: "Deleted!",
                    text: "Your item has been deleted.",
                    icon: "success"
                }).then(() => {
                    document.getElementById(delete -form - ${ authorId }).submit();
                });
            }
        });
    }
</script>

@section Scripts {
    <script>
        $(document).ready(function () {
            $('#autoresTable').DataTable({
                "language": {
                    "lengthMenu": "Mostrar _MENU_ entradas por página",
                    "zeroRecords": "No se encontraron resultados",
                    "info": "Mostrando _START_ a _END_ de _TOTAL_ entradas",
                    "infoEmpty": "Mostrando 0 a 0 de 0 entradas",
                    "infoFiltered": "(filtrado de _MAX_ entradas totales)",
                    "search": "Buscar:",
                    "paginate": {
                        "first": "Primero",
                        "last": "Último",
                        "next": "Siguiente",
                        "previous": "Anterior"
                    }
                }
            });
        });
    </script>
}

            </td>
        </tr>
}
    </tbody>
</table>
<style>
    body {
        background-color: #ede7f6; /* Color de fondo morado claro */
    }

    h1 {
        color: #5c2f77; /* Color morado oscuro */
        text-align: center;
        margin-top: 20px;
    }

    .btn-primary {
        background-color: #7e57c2; /* Color morado */
        border-color: #7e57c2;
        color: #ffffff;
    }

        .btn-primary:hover {
            background-color: #6c3e8e; /* Color morado claro */
            border-color: #6c3e8e;
            opacity: 0.8;
        }

    .btn-create {
        background-color: #9575cd; /* Color morado claro */
        border-color: #9575cd;
        color: #ffffff;
    }

        .btn-create:hover {
            background-color: #8461c6; /* Color morado más oscuro */
            border-color: #8461c6;
            opacity: 0.8;
        }

    .btn-danger {
        background-color: #f48fb1; /* Color rosa claro */
        border-color: #f48fb1;
        color: #ffffff;
    }

        .btn-danger:hover {
            background-color: #f06292; /* Color rosa más oscuro */
            border-color: #f06292;
            opacity: 0.8;
        }

    .thead-light {
        background-color: #d1c4e9; /* Color morado claro */
    }

    .table-striped tbody tr:nth-of-type(odd) {
        background-color: #e1bee7; /* Color morado muy claro */
    }

    .table {
        background-color: #f5f5f5; /* Color de fondo de la tabla */
    }

    .shadow {
        box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15) !important;
    }

    .rounded {
        border-radius: 0.25rem !important;
    }

    .btn-group a {
        margin-right: 10px;
    }
</style>